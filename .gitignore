# Local editor and tool config, not part of the site
.cursor/
.mcp.json

# CV source file: contains phone number and address, keep off the public site
Gao_CV_updated.docx
*.docx

# Design reference image (Canva template), not published
Blush Bronze Website Launch Promo Instagram Post.png

# Full-size photo source. The site serves the optimized copies in images/.
/Gao_2022.jpg

# Jekyll build output, if a local build is ever run
_site/
.jekyll-cache/
vendor/
Gemfile.lock
